interface Session {
  id: string;
  client_id?: string;
  specialist_id?: string;
  data_hora?: string;
  tipo_sessao?: string;
  status?: string;
  clientName?: string;
  // Outros campos que podem existir
}
